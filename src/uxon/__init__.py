"""uxon — multi-user wrapper for terminal AI coding agent sessions.

The single source of truth for the package version. Hatch reads this via
``[tool.hatch.version].path`` so wheels, sdists, the in-tree CLI, and
``uxon version`` all agree without a separate ``VERSION`` file.
"""

__version__ = "3.3.0.dev3"
