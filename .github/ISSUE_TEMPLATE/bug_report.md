---
name: Bug report
about: Report something that doesn't behave the way the docs say it should
labels: bug
---

### What happened?

<!-- Exact symptom and the command that triggered it. -->

```
$ uxon <args>
<output>
```

### What did you expect?

<!-- One or two sentences. -->

### `uxon doctor` output

```
$ uxon doctor
<paste output>
```

### Environment

- `uxon --version`:
- OS / distro:
- Python version (`python3 --version`):
- `tmux -V`:
- Agent involved (`claude` / `codex` / `cursor-agent`) and version:
- Inside another `tmux` / `screen` / `mosh` session? (yes/no)

### Anything else

<!-- Logs (`~/.local/state/uxon/...`), config snippets, screenshots. Redact secrets. -->
